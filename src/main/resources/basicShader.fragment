#version 330 core

in vec2 fTexCoords;

out vec4 color;

uniform sampler2D TEX_SAMPLER;

void main()
{
     // float rBase = 0.01;
     // float gBase = 0.1;
     // float bBase = 0.5;
     // color = vec4(rBase,gBase,bBase, 1.0);

     color = texture(TEX_SAMPLER, fTexCoords);
}